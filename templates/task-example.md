Цель: Добавить кэширование результатов в API-эндпоинт /search

Контекст: Node.js + Express, файлы в src/routes/search.js и
src/services/searchService.js. Нельзя менять публичный контракт эндпоинта.
Redis уже подключён в проекте (src/lib/redis.js).

Ты — Opus, оркестратор и главный исполнитель. Сложную/архитектурную часть
(стратегию кэширования, TTL, инвалидацию) делай сам. Перед делегированием
подзадач и перед подключением skill — задай Jev атомарный typed-вопрос
(jev-route.sh / jev-agent-skill); итоговое решение и рискованные вызовы
всё равно проверяй детерминированным кодом. Рутину (тесты, форматирование)
отдавай ojc-boilerplate-executor, тривиальные задачи — ojc-quick-helper. После
завершения всей реализации — один раз финальное Codex-ревью
(/codex:review или /codex:adversarial-review), закрой замечания и проверь
исправления тестами, без повторного ревью.

Сначала покажи мне план, затем приступай к выполнению.
